export const renderListItem = (template, item) => ``;
export const renderFormControl = (template, model) => ``;
export const renderSelectable = (template, options) => ``;
export const renderSwitchable = (template, options) => ``;
export const renderClickable = (template, action) => ``;

export const makeDraggable = (item, options) => ``;
export const makeSelectable = (item, options) => ``;
